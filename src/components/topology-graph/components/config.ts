const NodeSize = 80;
// 单元格
const CELL = {
    WIDTH: 90,
    HEIGHT: 120,
};
// 图标
const ICON = {
    WIDTH: 60,
    HEIGHT: 60,
};
// 顶部信息
const TOP_LABEL = {
    WIDTH: CELL.WIDTH,
    HEIGHT: 20,
};
// 底部信息
const BOTTOM_LABEL = {
    WIDTH: CELL.WIDTH,
    HEIGHT: 20,
};
const LABEL_HEIGHT = 20;


export {
    NodeSize,
    CELL,
    ICON,
    TOP_LABEL,
    BOTTOM_LABEL,
    LABEL_HEIGHT
}